<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fundo"
    android:padding="32dp">

    <!-- Ícone de carro centralizado no topo -->
    <ImageView
        android:id="@+id/ivStatus"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:src="@drawable/ic_car_lock"
        android:contentDescription="@string/desc_icone_status"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.12"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Nome do app -->
    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="@color/texto_primario"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/ivStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Status da conexão (Android Auto conectado / desconectado) -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/status_verificando"
        android:textSize="15sp"
        android:textColor="@color/texto_secundario"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Card explicativo central -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardExplicacao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="36dp"
        app:cardBackgroundColor="@color/fundo_card"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvInstrucao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp"
            android:text="@string/instrucao_desconectado"
            android:textSize="14sp"
            android:textColor="@color/texto_secundario"
            android:lineSpacingMultiplier="1.5"
            android:gravity="center" />

    </androidx.cardview.widget.CardView>

    <!-- Botão para ativar/desativar -->
    <Button
        android:id="@+id/btnToggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_desativar"
        android:textSize="15sp"
        android:paddingHorizontal="40dp"
        android:paddingVertical="14dp"
        app:layout_constraintTop_toBottomOf="@id/cardExplicacao"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.3"
        android:layout_marginTop="32dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
