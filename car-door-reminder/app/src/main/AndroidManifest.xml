<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth: API <= 30 usa permissão legada, API 31+ usa BLUETOOTH_CONNECT -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />

    <!-- Vibrar ao alertar -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Mostrar notificações (Android 13+) -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />

    <!-- Permite pedir exclusão da otimização de bateria -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarDoorReminder">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            BluetoothReceiver: acorda o app automaticamente quando o Bluetooth
            do carro desconecta — funciona mesmo com o app completamente fechado.
            ACTION_ACL_DISCONNECTED está na lista de broadcasts isentos do
            Android 8+ e é entregue mesmo sem o app em memória.
        -->
        <receiver
            android:name=".BluetoothReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
            </intent-filter>
        </receiver>

        <!-- Receptor do segundo lembrete (30s após a desconexão) -->
        <receiver
            android:name=".SecondReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
